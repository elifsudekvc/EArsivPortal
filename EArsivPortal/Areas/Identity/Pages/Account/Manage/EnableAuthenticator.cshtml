@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "Configure authenticator app";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div>
    <p>Bir kimlik doğrulayıcı uygulaması kullanmak için aşağıdaki adımları izleyin:</p>
    <ol class="list">
        <li>
            <p>
                için Microsoft Authenticator gibi iki faktörlü bir kimlik doğrulayıcı uygulaması indirin
                <a href="https://go.microsoft.com/fwlink/?Linkid=825072">Android</a> ve
                <a href="https://go.microsoft.com/fwlink/?Linkid=825073">iOS</a> veya
                Google Authenticator için
                <a href="https://play.google.com/store/apps/details?id=com.google.android.apps.authenticator2&amp;hl=en">Android</a> ve
                <a href="https://itunes.apple.com/us/app/google-authenticator/id388497605?mt=8">iOS</a>.
            </p>
        </li>
        <li>
            <p>QR Kodunu tarayın veya bu anahtarı girin <kbd>@Model.SharedKey</kbd> iki faktörlü kimlik doğrulayıcı uygulamanıza girin. Boşluklar ve kasa önemli değildir.</p>
            <div id="qrCode"></div>
            <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
        </li>
        <li>
            <p>
                QR kodunu taradıktan veya yukarıdaki anahtarı girdikten sonra, iki faktörlü kimlik doğrulama uygulamanız size şunları sağlayacaktır
                benzersiz bir kod ile. Kodu aşağıdaki onay kutusuna girin.
            </p>
            <div class="row">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                            <input asp-for="Input.Code" class="form-control" autocomplete="off" placeholder="Please enter the code."/>
                            <label asp-for="Input.Code" class="control-label form-label">Verification Code</label>
                            <span asp-validation-for="Input.Code" class="text-danger"></span>
                        </div>
                        <button type="submit" class="w-100 btn btn-lg yesil">Doğrulama</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        $(() => {
            var qrcode = new QRCode(document.getElementById("qrCode"), {
                text: "@Html.Raw(Model.AuthenticatorUri)",
                width: 128,
                height: 128,
                colorDark: "#000000",
                colorLight: "#ffffff",
                correctLevel: QRCode.CorrectLevel.H
            });
        })
    </script>
}